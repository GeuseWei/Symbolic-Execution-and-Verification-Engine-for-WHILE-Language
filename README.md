ECE653 Fall 2023: Project repository

Enter your personal information in `group.yml`.

Place your code in `wlang` directory.

Place your report under the name `project.pdf` in the root of the repository.
